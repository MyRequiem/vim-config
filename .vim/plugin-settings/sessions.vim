scriptencoding utf-8

" отключаем загрузку плагина
" let g:loaded_sessions = 1

" полный путь для сохранения файлов сессий (default: $HOME/.vim/sessions)
" создается, если не существует
" let g:sessions_dir = $HOME . '/.vim/sessions'
